# Optional. If unset, chat uses the offline rule responder.
OPENAI_API_KEY=
# Optional OpenAI-compatible base (Together, Groq, local, etc.)
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o-mini
